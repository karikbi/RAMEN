# RAMEN Wallpaper Curation Pipeline - Dependencies

# =============================================================================
# Part 1: Fetching
# =============================================================================

# Reddit API
# Reddit API (Using public JSON, praw removed)

# Async HTTP requests
aiohttp>=3.9.0

# =============================================================================
# Part 2: Filtering, Quality Scoring, Embeddings
# =============================================================================

# Image processing
Pillow>=10.0.0
opencv-python>=4.8.0
imagehash>=4.3.0

# OCR for text detection (optional - requires tesseract binary)
pytesseract>=0.3.10

# Machine learning
numpy>=1.24.0
scikit-learn>=1.3.0

# Deep learning - TensorFlow CPU-only (for MobileNetV3, EfficientNetV2)
# Using CPU version to save ~4GB disk space on GitHub runners
tensorflow-cpu>=2.15.0

# Deep learning - PyTorch CPU-only (for SigLIP, DINOv2)
# Using CPU version to save ~2GB disk space on GitHub runners
torch>=2.1.0
torchvision>=0.16.0
# Note: CPU-only torch is installed by default. For CUDA, see notes at bottom.

# Hugging Face Transformers (for SigLIP)
transformers>=4.36.0

# Progress bars
tqdm>=4.66.0

# =============================================================================
# Part 3: Storage & Reporting
# =============================================================================

# AWS S3 SDK (compatible with Cloudflare R2)
boto3>=1.34.0

# =============================================================================
# Part 4: Robustness & Recovery
# =============================================================================

# System monitoring (memory, disk)
psutil>=5.9.0

# Bloom filter for fast deduplication (optional but recommended)
pybloom_live>=4.0.0


# =============================================================================
# Development
# =============================================================================

# Type checking
mypy>=1.0.0

# =============================================================================
# Part 5: Configuration & Testing
# =============================================================================

# Configuration
pyyaml>=6.0.0

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

# =============================================================================
# Notes
# =============================================================================
# 
# Optional system dependencies:
# - Tesseract OCR: brew install tesseract (macOS) or apt install tesseract-ocr (Linux)
# 
# GPU Support (Local Development Only):
# - By default, CPU-only versions are installed to save disk space
# - For CUDA: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# - For MPS (Apple Silicon): Works automatically with PyTorch 2.0+
# - For TensorFlow GPU: pip install tensorflow[and-cuda]
# 
# CI/CD (GitHub Actions):
# - Uses CPU-only versions to fit within free tier disk limits
# - Disk cleanup step removes ~30GB of unnecessary software
# 
# To install all dependencies:
#   pip install -r requirements.txt
# 
# To run tests:
#   pytest tests/ -v


